{% extends 'base.html.twig' %}

{% block title %}
    Challenger 🔥 
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('node_modules/bootstrap/dist/css/bootstrap.min.css') }}" type="text/css">
    <link rel="stylesheet" href="{{ asset('node_modules/select2/dist/css/select2.min.css') }}" type="text/css"/>
    <link rel="stylesheet"
          href="{{ asset('node_modules/select2-bootstrap-5-theme/dist/select2-bootstrap-5-theme.rtl.min.css') }}"
          type="text/css"/>
    <link href="{{ asset('styles/styles.css') }}" rel="stylesheet" type="text/css"/>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('node_modules/select2/dist/js/select2.full.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('js/scripts.js') }}" type="text/javascript"></script>
{% endblock %}

{% block body %}
    <body>
    <div id="content">
        <nav class="sb-topnav navbar navbar-expand navbar-dark bg-dark">
            <!-- Navbar Brand-->
            <a class="navbar-brand" href="{{ path('app.home.muscu') }}">Challenger🔥 </a>
            <!-- Sidebar Toggle-->
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <button class="btn btn-link " id="sidebarToggle" href="#!"><i
                            class="fas fa-bars"></i></button>
            {% endif %}

            <ul class="navbar-nav ">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button"
                       data-bs-toggle="dropdown"
                       aria-expanded="false">{{ (app.user)?app.user.prenom:"" }}<i class="fas fa-user fa-fw"></i></a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <li><a class="dropdown-item" href="{{ path('app.home.muscu') }}">Challenge</a></li>
                            <li><a class="dropdown-item" href="{{ path('app.logout') }}">Paramètres</a></li>
                            <li><a class="dropdown-item" href="{{ path('app.logout') }}">Me déconnecter</a></li>
                        {% else %}
                            <li><a class="dropdown-item" href="{{ path('app.login') }}">Me connecter</a></li>
                            {#                        <li><a class="dropdown-item" href="{{ path('app.register') }}">Register</a></li> #}
                        {% endif %}

                    </ul>
                </li>
            </ul>
        </nav>
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <div id="layoutSidenav">
                <div id="layoutSidenav_nav">
                    <nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
                        <div class="sb-sidenav-menu">
                            <div class="nav">
                                <div class="sb-sidenav-menu-heading">Ma routine</div>
                                <a class="nav-link" href="{{ path('savers.insert') }}">
                                    <div class="sb-nav-link-icon"><i class="fa-solid fa-dragon"></i></div>
                                    S.A.V.E.R.S
                                </a>
                                <div class="sb-sidenav-menu-heading">Mon challenge</div>
                                <a class="nav-link" href="{{ path('seances.day') }}">
                                    <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
                                    Ma séance
                                </a>
                                <div class="sb-sidenav-menu-heading">Admin</div>

                                <a class="nav-link" href="{{ path('exercice.edit') }}">Création exercice </a>
                                <a class="nav-link" href="{{ path('programme.edit') }}">Création programme </a>
                                <a class="nav-link" href="{{ path('seances.edit') }}">Création seance</a>
                                <div class="sb-sidenav-menu-heading">Contenu</div>
                                <a class="nav-link" href="{{ path('seances.day') }}">
                                    <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
                                    Seances
                                </a>
                                <a class="nav-link" href="{{ path('programme.show') }}">
                                    <div class="sb-nav-link-icon"><i class="fas fa-table"></i></div>
                                    Programmes
                                </a>
                                <a class="nav-link" href="{{ path('exercice.all') }}">
                                    <div class="sb-nav-link-icon"><i class="fa-solid fa-hand-fist"></i></div>
                                    Exercices
                                </a>
                            </div>
                        </div>
                    </nav>
                </div>
            </div>
        {% endif %}
        <!--- message_session ---->
        {% for message in app.session.flashBag.get('info') %}
            <br>
            <div class="row alert alert-info">
                {{ message }}
            </div>
        {% endfor %}
        {% for message in app.session.flashBag.get('success') %}
            <br>
            <div class="row alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
        {% for message in app.session.flashBag.get('error') %}
            <br>
            <div class="row alert alert-danger">
                {{ message }}
            </div>
        {% endfor %}

        {% block content %}

        {% endblock %}
    </div>
    </body>
{% endblock %}
