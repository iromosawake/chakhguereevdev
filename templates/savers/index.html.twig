{% extends 'hommeultime.html.twig' %}

{% block title %}S.A.V.E.R.S{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://unpkg.com/bootstrap-table@1.22.1/dist/bootstrap-table.min.css" rel="stylesheet">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://unpkg.com/bootstrap-table@1.22.1/dist/bootstrap-table.min.js"></script>
    <script src="https://unpkg.com/bootstrap-table@1.22.1/dist/extensions/mobile/bootstrap-table-mobile.min.js"></script>
    <script>
        $(function () {
            $('#table').bootstrapTable()
        })
    </script>
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col mt-2">
            <button type="button" class="btn btn-info" data-toggle="modal" data-target=".bd-affirmations-modal-lg">Mes affiramtions positives</button>
        </div>
        <div class="modal fade bd-affirmations-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-lg">
                <div class="modal-content">
                    Here man
                </div>
            </div>
        </div>
    </div>
<div class="row">
        <div class="col-lg-6 mt-3">
            {{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_row(form.silence) }}
            {{ form_row(form.affirmations) }}
            {{ form_row(form.visualisation) }}
            {{ form_row(form.exercice) }}
            {{ form_row(form.reading) }}
            {{ form_row(form.scribing) }}
            {{ form_rest(form) }}
            <div class="row text-center mb-4">
                <button type="submit" class="btn btn-dark ">J'ai terminé ma routine</button>
            </div>
            {{ form_end(form) }}
        </div>

        <div class="col-lg-6">
            <table
                    class="table table-striped table-dark"
                    id="table"
                    data-mobile-responsive="true">
                <thead>
                <tr>
                    <th scope="col" data-field="date">Date</th>
                    <th scope="col" data-field="s">S</th>
                    <th scope="col" data-field="a">A</th>
                    <th scope="col" data-field="v">V</th>
                    <th scope="col" data-field="e">R</th>
                    <th scope="col" data-field="r">R</th>
                    <th scope="col" data-field="sc">S</th>
                </tr>
                </thead>
                <tbody>
                {% for  saver in saverses %}
                    <tr class="table">
                        <td>{{ saver.createdAt | date('d-m-Y') }} {{ saver.createdAt | date('H:i') }}</td>
                        <td>{{ (saver.silence)?"👌🏻 ":"🙄 " }}</td>
                        <td>{{ (saver.affirmations)?"👌🏻 ":"🙄 " }}</td>
                        <td>{{ (saver.visualisation)?"👌🏻 ":"🙄 " }}</td>
                        <td>{{ (saver.exercice)?"👌🏻 ":"🙄 " }}</td>
                        <td>{{ (saver.reading)?"👌🏻 ":"🙄 " }}</td>
                        <td>{{ (saver.scribing)?"👌🏻 ":"🙄 " }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
